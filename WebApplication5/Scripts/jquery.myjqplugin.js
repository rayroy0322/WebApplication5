(function ($) {
    $.fn.tooltip = function (options) {
    };
    //等價於
    var tooltip = {
        function(options) {
            alert(123)
        }
    };
    $.fn.extend(tooltip) = $.prototype.extend(tooltip) = $.fn.tooltip
})(jQuery);